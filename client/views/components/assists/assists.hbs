<table class="component component-big" id="assists-{{league.code}}">
    <tr>
        <td colspan="10">
            <div class="component-title">
                <img src={{league.logo}} />
                <h1>{{league.name}} top assists</h1>
            </div>
        </td>
    </tr>
    <tr>
        <th title="Position">#</th>
        <th class="component-main-column" title="Player name">Name</th>
        <th class="component-main-column" title="Team">Team</th>
        <th title="Goals">Goals</th>
    </tr>
    {{#each data}}
    <tr>
        <td>{{this.rank}}</td>
        <td class="component-main-column">
            <span>{{this.name}}</span>
        </td>
        <td class="component-main-column">
            <img src={{this.logo}} onerror="this.style.display='none'" />
            <span>{{this.team}}</span>
        </td>
        <td>{{this.goals}}</td>
    </tr>
    {{/each}}
</table>