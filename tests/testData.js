'use strict';

var assert = require('chai').assert;
var config = require('../server/config');
var helper = require('../server/helper');
var leagues = require('../server/data/leagues');
var competitions = require('../server/data/competitions');

describe('Data intergrity', function () {
    describe('leagues', function () {
        describe('table', function () {
            it('should have the right number of teams', function () {
            });
        });
    });

    describe('competitions', function () {
    });
});